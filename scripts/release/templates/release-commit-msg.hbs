chore(release): publish [skip ci]
{{#if releasedPackages}}

{{#each releasedPackages}}
## 📦 {{this.name}}@{{lookup @root.releasedVersions this.name}}

{{#each (lookup @root.releasedCommits this.name)}}
- 🔹 {{this.type}}{{#if this.isBreaking}}!{{/if}}: {{this.subject}}
{{/each}}
{{#each (call this "getDependencyUpdates" @root.releasedVersions)}}
- 📦 feat({{this.packageName}}): update from {{this.oldVersion}} to {{this.newVersion}}
{{/each}}

{{/each}}
Affected: 📌 {{#each releasedPackages}}{{this.name}}{{#unless @last}},{{/unless}}{{/each}}
{{/if}}
