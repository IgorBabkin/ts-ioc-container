name: Download build artifacts
description: Restores the cjm/esm/typings output uploaded by upload-build-artifacts

runs:
  using: composite
  steps:
    - name: Download build artifacts
      uses: actions/download-artifact@v4
      with:
        name: build-output
